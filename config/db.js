const MySQLStore = require('express-mysql-session');
const db = require('../config/db'); // db.js config file
module.exports = {
    host: 'localhost',
    database: 'organic',
    username: 'organic',
    password: 'green'
}